name: Video Crop + Waveform + Upload to Google Drive

on:
  workflow_dispatch:
    inputs:
      video_url:
        description: 'Video URL'
        required: true
      drive_folder_id:
        description: 'Google Drive folder ID to upload video'
        required: true
      video_name:
        description: 'Name for the uploaded video'
        required: true

jobs:
  process-video:
    runs-on: ubuntu-latest
    outputs:
      drive_url: ${{ steps.upload.outputs.drive_url }}
      videoFileId: ${{ steps.upload.outputs.videoFileId }}

    steps:
      - name: Install FFmpeg + Python deps
        run: |
          sudo apt update
          sudo apt install -y wget python3-pip
          pip3 install requests
          # تنزيل نسخة حديثة من FFmpeg
          wget -O ffmpeg-release.tar.xz https://johnvansickle.com/ffmpeg/releases/ffmpeg-release-amd64-static.tar.xz
          tar -xvf ffmpeg-release.tar.xz
          export PATH="$PWD/ffmpeg-*-amd64-static:$PATH"

      - name: Download video
        run: |
          wget -O input.mp4 "${{ github.event.inputs.video_url }}"

      - name: Crop, Scale & Add Waveform
        run: |
          ./ffmpeg-*-amd64-static/ffmpeg -i input.mp4 -filter_complex "
            [0:v]crop=1280:545,scale=1280:1080,setsar=1[v];
            [0:a]aformat=channel_layouts=mono,showwaves=s=1280x400:mode=line:colors=white@1.0:scale=lin[wave];
            [v][wave]overlay=(W-w)/2:H-h-50
          " -c:a aac -c:v libx264 -crf 23 output_final.mp4

      - name: Upload to Google Drive
        id: upload
        env:
          CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          REFRESH_TOKEN: ${{ secrets.GOOGLE_REFRESH_TOKEN }}
          DRIVE_FOLDER_ID: ${{ github.event.inputs.drive_folder_id }}
          VIDEO_NAME: ${{ github.event.inputs.video_name }}
        run: |
          python3 <<'EOF'
          import os, requests, json, sys

          client_id = os.environ["CLIENT_ID"]
          client_secret = os.environ["CLIENT_SECRET"]
          refresh_token = os.environ["REFRESH_TOKEN"]
          folder_id = os.environ["DRIVE_FOLDER_ID"]
          video_name = os.environ["VIDEO_NAME"]

          # طلب access_token جديد باستخدام refresh_token
          token_resp = requests.post(
              "https://oauth2.googleapis.com/token",
              data={
                  "client_id": client_id,
                  "client_secret": client_secret,
                  "refresh_token": refresh_token,
                  "grant_type": "refresh_token"
              }
          )
          if token_resp.status_code != 200:
              print("Token error:", token_resp.text)
              sys.exit(1)

          access_token = token_resp.json()["access_token"]
          headers = {"Authorization": f"Bearer {access_token}"}

          # البحث عن أي ملف بنفس الاسم في المجلد المحدد وحذفه
          search_url = f"https://www.googleapis.com/drive/v3/files?q=name='{video_name}' and '{folder_id}' in parents&fields=files(id,name)"
          resp = requests.get(search_url, headers=headers)
          if resp.status_code == 200:
              for f in resp.json().get("files", []):
                  requests.delete(f"https://www.googleapis.com/drive/v3/files/{f['id']}", headers=headers)

          # رفع الفيديو الجديد
          metadata = {"name": video_name, "parents": [folder_id]}
          files = {
              "metadata": ("metadata", json.dumps(metadata), "application/json"),
              "file": open("output_final.mp4", "rb"),
          }
          upload = requests.post(
              "https://www.googleapis.com/upload/drive/v3/files?uploadType=multipart",
              headers=headers,
              files=files,
          )
          result = upload.json()
          file_id = result.get("id")
          if not file_id:
              print("Upload failed:", result)
              sys.exit(1)

          # إخراج مخرجات GitHub Actions
          with open(os.environ["GITHUB_OUTPUT"], "a") as f:
              f.write(f"videoFileId={file_id}\n")
              f.write(f"drive_url=https://drive.google.com/file/d/{file_id}/view\n")
          EOF
